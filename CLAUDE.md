# CLAUDE.md

Agent rules for this repo live in `~/.claude/CLAUDE.md` — working style, code standards, git workflow, doc comments, plus the two knowledge-tool workflows:

- **Graphify** (`## Graphify`) — query the `graphify-out/` knowledge graph before raw source browsing; `graphify update .` after code changes. Not generated in this repo yet: run `graphify update .` to create it.
- **Memory** (`## Memory (remember tools)`) — persistent file memory for this project at `~/.claude/projects/-Users-chazuka-Works-chatty-ocbc-id/memory/`, indexed by `MEMORY.md`.

Project-specific rules go here, below this line. Nothing yet.
