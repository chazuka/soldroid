// Root build file. It applies nothing: every plugin is applied by a module, or by one of the
// convention plugins in the included `build-logic` build. Declaring them here with `apply false`
// would be a second place to change a version, and the version catalog is already the first.
